[package]
name = "dbmeta"
version = "0.0.1"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }

tokio = { version = "1", features = ["rt", "macros"] }

tokio-postgres = "0.7"

serde = { version = "1", features = ["derive"] }
serde_json = "1"

anyhow = "1"
